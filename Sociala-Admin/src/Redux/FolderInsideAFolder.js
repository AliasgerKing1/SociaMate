import {createSlice} from "@reduxjs/toolkit";

let initialState = [];

let folderInsideAFolderReducer = createSlice({
    name : "Folder Inside A Folder",
    initialState,
    reducers: {
        
    }
})